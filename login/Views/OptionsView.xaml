<UserControl x:Class="CryptorLogin.Views.OptionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:syncfusion="clr-namespace:Syncfusion.Windows.PdfViewer;assembly=Syncfusion.PdfViewer.WPF"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="📂 Вибрати файл" Click="PickFile_Click" Height="20" Width="200"/>
        <Grid Grid.Row="1" x:Name="ViewerContainer"/>
        <!--<StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <Button Content="📂 Вибрати файл" Click="PickFile_Click" Height="20" Width="200"/>
            --><!-- Контейнер для відображення документа --><!--
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="2">
                <Grid x:Name="ViewerContainer"/>
            </Border>
        </StackPanel>-->

       
    </Grid>
</UserControl>
